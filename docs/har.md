# HAR notes

The latest `skylight.har` capture includes API calls to `https://app.ourskylight.com/api/...` (good).

This is the host we care about for agent endpoints:

- `https://app.ourskylight.com/api/...` (or proxied `/api/...`)

We keep request-shape findings in `docs/findings.md`.
